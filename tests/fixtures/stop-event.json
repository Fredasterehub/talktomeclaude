{
  "session_id": "9f8c2f6e-4d1a-4b7e-b1c3-0a2d9e8f7c65",
  "transcript_path": "tests/fixtures/transcript.jsonl",
  "cwd": "/home/dev/project",
  "permission_mode": "default",
  "hook_event_name": "Stop",
  "stop_hook_active": false,
  "last_assistant_message": "The stale fixture was the culprit. I also hardened the guard helper so tests can never invoke it directly:\n\n```python\ndef forbidden_function(path):\n    raise RuntimeError(\"never call this from tests\")\n```\n\nAll twelve tests pass now. The suite is clean and the scratch directory is recreated fresh on each run."
}
