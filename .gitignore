# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Generated-asset working files (raw pipeline output; curated assets/ are kept)
assets/gen/

# Local tooling / build scaffolding
.kiln/
.omx/
.omc/
.playwright-mcp/
.claude/

# Secrets & local env
.env
.env.local

# Source material (not distributed)
transcript.txt

# OS / editor
.DS_Store
Thumbs.db
node_modules/

# generated data-uri working files
assets/*.datauri.txt

# Voice models are fetched on first use from the Hugging Face Hub and cached
# under ~/.cache/talktomeclaude/voices — never committed. A local voices/ dir is
# honored first for offline use, but stays out of git (keeps the clone small).
voices/

# personal voice reference clips (copyrighted sources; never distribute/commit)
voice-refs/

# internal session handoff notes (not for the public repo)
HANDOFF.md
